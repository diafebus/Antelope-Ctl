{
  "_comment": "STUB (2026-08-31). Catalogue of Antelope mic-modelling ('emuMic') models for the Orion Studio III's per-preamp mic emulation. This file is intentionally separate from the device profile: the model list a given user can actually load depends on which Antelope modelling mic + model packs are registered to THEIR Antelope account, so a client should pick a model set per user, not per device. The DEVICE-SIDE control of the modelling DSP (enable / polar pattern / capsule swap) is in profiles/orion_studio_3.json -> frame.micmodeling_command + params.mic_modeling; THIS file is only about the model-selection layer.",

  "status": "structure only -- model IDs NOT yet captured",

  "what_is_known": {
    "dsp_control_frame": "opcode 0x17 / param_id 0xe5 (frame.micmodeling_command in orion_studio_3.json). Controls enable, polar pattern (0-100 continuous: 0 omni / 50 cardioid / 100 bi-directional), and a channel-order swap switch. Present on preamps 7-12 only.",
    "matrix_source": "the modelled (wet) signal is exposed as routing source bank 0x01 ('emumic'), distinct from bank 0x00 (dry preamp) -- see routing_command.source_banks. Bank index base still unconfirmed (CAPTURE C).",
    "model_selection_frame": "UNKNOWN -- not in macos-ch7-8-micmodeling-* / macos-ch9-10_11-12-micmodeling-* (those only moved pattern + swap + enable). Needs a capture of the model dropdown being changed on a modelling-capable preamp. Likely another 0x17/0xe5 sub-shape or a SET_PARAM with a model-id byte.",
    "side_effects_of_enabling": "the Launcher also turns on 48V phantom (param 0x51) for the pair and links the preamp pair (SET_LINK space 0)."
  },

  "account_binding": {
    "note": "Antelope Edge / Verge modelling mics and the model/emulation packs are activated online against an Antelope account. A model the user does not own will not load. A client should therefore let the user declare which mic(s) + packs they have (or, better, read the entitlement list if that ever proves reachable -- it is NOT reachable from the HID interface, which has no readback at all) and only offer those models.",
    "do_not": "do not ship the licensed emulation model list scraped from Antelope's software, and never touch activation/entitlement traffic -- same policy as the AFX plugin chain (see CLAUDE.md 'AFX / Synergy Core plugins -- plan of attack', buckets E/F)."
  },

  "modelling_mics": {
    "_comment": "The Antelope modelling-mic hardware families, from public product pages (facts only). Fill in per-user which one(s) they own. The Orion's emuMic works with any of these feeding preamps 7-12.",
    "edge_solo": { "capsules": 1, "pattern_control": false, "notes": "single large-diaphragm modelling mic" },
    "edge_duo":  { "capsules": 2, "pattern_control": true,  "notes": "dual-capsule; the continuous polar-pattern morph (0 omni / 50 cardioid / 100 figure-8) and the channel-order swap apply here" },
    "edge_note": { "capsules": 1, "pattern_control": false, "notes": "small-diaphragm pencil, matched-pair capable" },
    "edge_go":   { "capsules": 1, "pattern_control": false, "notes": "USB modelling mic" },
    "verge":     { "capsules": 1, "pattern_control": false, "notes": "compact modelling mic, matched-pair capable" }
  },

  "user_owned": {
    "_comment": "Set this per install. The repo owner (diafebus) has an Edge Duo.",
    "mics": ["edge_duo"],
    "model_packs": []
  },

  "models": {
    "_comment": "model_id -> {name, requires} once captured. model_id 0 = OFF / no model (seen as the reset value when modelling is disabled, though that path actually used param 0x51 = phantom, so even '0 = off' is inferred, not confirmed). Everything else TBD from a model-dropdown capture.",
    "0": { "name": "off", "requires": null }
  }
}
