# Antelope Audio Orion Studio Synergy Core / OrionStudio III
#
# USB-Audio.conf includes this optional per-device file before it selects a
# profile.  Keep the USB ID here; the CardLongName contains a machine-specific
# USB topology suffix and is not a stable lookup key.

Define.ProfileName "Antelope/OrionStudio-III"
